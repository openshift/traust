---
description: "Summarize recurring insecure coding patterns across portfolio audit findings."
---

Build the cross-portfolio insecure-coding-pattern roll-up using the insecure-patterns skill. Load the skill at .claude/skills/insecure-patterns/SKILL.md and follow its full procedure — resolving the analysis-results root, running harnessing/insecure-patterns/scripts/build_insecure_patterns.py with the flags implied by $ARGUMENTS (summary / open / explicit results-root path), and reporting the top-25 pattern table, corpus stats, and output file paths back to the user.
