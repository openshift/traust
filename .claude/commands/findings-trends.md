Build the portfolio findings trends dashboard using the findings-trends skill. Load the skill at .claude/skills/findings-trends/SKILL.md and follow its full procedure — run harnessing/findings-trends/scripts/build_trends.py with $ARGUMENTS (optional: --granularity month|week, --since YYYY-MM, --product <name>, --as-of YYYY-MM-DD), which replays every repo's track-findings disposition ledger against time buckets and writes findings-trends.{json,md,html} to analysis-results/trends/. Then read findings-trends.md back to the user: lead with the direction table (open findings, CVSS-sum risk index, remediation velocity, MTTR, new findings, regressions, FP rate, accepted risk index, ledger coverage), call out any worsening ⚠ metrics and the Accepted Risk Register (formally accepted findings — unfixed and unmitigated), and state the ledger-coverage caveat: ledger-less repos count as open by design.
