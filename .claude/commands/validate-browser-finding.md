---
description: "Validate CSRF, XSS, or clickjacking findings against explicitly authorized browser targets."
---

Validate a browser-exploitable web finding (CSRF, XSS, clickjacking) against an authorized live target using the validate-browser-finding skill. Load the skill at .claude/skills/validate-browser-finding/SKILL.md and follow its full procedure, including its scope guard and Playwright MCP requirements. `$ARGUMENTS` is the audit report (and optionally the finding id) to validate.
