Run the detection-recall benchmark using the recall-benchmark skill. Load the skill at .claude/skills/recall-benchmark/SKILL.md and follow its full procedure for the mode implied by $ARGUMENTS (bootstrap / admit / run / score, with --sample and --release as given) — honoring the contamination rule (audit agents never learn they are benchmarked; the manifest never enters an audited tree) and the held-out rule (held-out targets scored only with --release). Report strict recall overall and per CWE class, the development-vs-held-out gap, stability, skipped targets, and admission provenance.
