Run a compliance assessment using the compliance-check skill. Load the skill at .claude/skills/compliance-check/SKILL.md and follow its full procedure — resolve the target (product via repo-graph / environment via targets.yaml), run the mandatory ADR staleness check, collect snapshots (skip-and-record), run harnessing/3-audit/compliance-check/scripts/run_compliance_check.py with the framework selection from $ARGUMENTS (one id, comma list, or "all" with per-framework precondition skipping), judge only evidence_review controls (twice; disagreement stays not_assessed), re-run the citation gate after any edits, and report with coverage honesty per framework (no blended percentage, skipped frameworks named, mandatory caveats kept). Declared inputs (PCI CDE boundary, GDPR personal-data stores, SOC 2 trust categories) come from the user, never inference.
