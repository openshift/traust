---
description: "Report campaign cost estimates, refresh spend records, or reconcile them against an invoice."
---

Report or refresh campaign financials for $ARGUMENTS using the financial-tracking skill. Load the skill at .claude/skills/financial-tracking/SKILL.md and follow its full procedure — starting with its Step 0 honesty preamble, which is not optional: every figure is a **list-price estimate, never an invoiced actual**, `claude-opus-5`'s rate is marked unverified in `config/model-registry.yaml` and carries most campaign spend, and business-unit coverage is partial (~35% of period cost resolves; the rest stays `bu-unknown` and is never redistributed to make a chart total 100%). `$ARGUMENTS` is `[report|capture|bu|reconcile]` plus optional `--month YYYY-MM`, `--months N`, `--invoice <csv>`. `report` (default) reads the metrics ledger only and is fast; `capture` first refreshes the chain (`collect_session_spend.py --append` → `attribute_session_spend.py --append` → `--reconcile`, whose non-zero exit is a hard stop meaning tokens were lost between collector and split); `bu` adds the business-unit split via `--by-bu` (walks transcripts, ~30s, resolved through findings.db `business_unit`); `reconcile` compares estimates against an operator-supplied invoice CSV (`period,amount_usd[,note]`). When answering "are we over budget", compare the **lane** subtotal — not the workstation total — against `monthly_budget.usd_central`, and state that the policy runs `enforcement: observe`, so nothing is actually being withheld. Finish by printing the period total with its lane/unattributed split, the trend direction, and every caveat that applies.
